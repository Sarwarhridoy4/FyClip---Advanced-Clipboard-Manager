# Build outputs - dist folder is kept for releases
# Files in dist/ with version numbers are tracked
dist/

# Build artifacts to ignore (not versioned)
*.exe
*.log
FyClip*
FyClip.AppDir*
*AppImage
*fyclip-deb*
*.test
fyclip
coverage.out

# Go build
vendor/
*.test
